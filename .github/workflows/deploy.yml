name: Deploy Magoya Knowledge Experience to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Stage site (drop repo-only files)
        run: |
          mkdir _site
          rsync -a --exclude='_site' --exclude='.git' --exclude='.github' \
            --exclude='build_pdf.py' --exclude='README.md' --exclude='.gitignore' \
            ./ _site/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
